# BluefinDecryptx SDK

Decryptx APIs client, generated from the OpenAPI spec.

This page describes the Bluefin Partner Application Programming Interface (API) for the Decryptx service.

Learn more about Decryptx APIs at [secure-prod.decryptx.com](https://secure-prod.decryptx.com).

This is an unofficial SDK for the Decryptx APIs public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

> TypeScript, Python, PHP, Golang, Ruby, Lua, C, Clojure, C++, C#, Dart, Elixir, Haskell, Java, JavaScript, Kotlin, OCaml, Perl, Rust, Scala, Swift, Zig SDKs, a CLI, an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

## Entities, not endpoints

This SDK exposes the API as a small set of **semantic entities** — Decrypt, DecryptExt and Validation — that you
call directly, instead of assembling URL paths and query strings. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `create`):

```ts
const client = new BluefinDecryptxSDK()
const items = await client.Decrypt().list()
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
const client = BluefinDecryptxSDK.test()
const decrypts = await client.Decrypt().list()
// decrypts is an array of bare Decrypt records populated with mock data
console.log(decrypts)
```

### Python

```python
client = BluefinDecryptxSDK.test()
decrypts = client.Decrypt().list()
print(decrypts)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = BluefinDecryptxSDK::test([
    "entity" => ["decrypt" => ["test01" => []]],
]);
$decrypts = $client->Decrypt()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.Decrypt(nil).List(
    nil, nil,
)
```

### Ruby

```ruby
# Seed fixture data so offline calls resolve without a live server.
client = BluefinDecryptxSDK.test({
  "entity" => { "decrypt" => { "test01" => {} } },
})
decrypts = client.Decrypt.list()
```

### Lua

```lua
local client = sdk.test()
local results, err = client:Decrypt():list()
```

### C

```c
#include "core/api.h"

BluefinDecryptxSDK* client = test_sdk(NULL, NULL);
PNError* err = NULL;
Entity* decrypt = bluefin_decryptx_decrypt(client, NULL);
voxgig_value* decrypts = decrypt->vt->list(decrypt, NULL, NULL, &err);
printf("%s\n", voxgig_to_json(decrypts));
```

### Clojure

```clojure
(require '[sdk.api :as api]
         '[sdk.entity.decrypt :as e-decrypt]
         '[voxgig.struct :as vs])

(def client (api/test-sdk nil nil))
(def decrypts (e-decrypt/list (api/decrypt client nil) nil nil))
(println decrypts)
```

### C++

```cpp
auto client = BluefinDecryptxSDK::testSDK();
Value decrypts = client->decrypt()->list(Value::undef(), Value::undef());
std::cout << Struct::jsonify(decrypts) << std::endl;
```

### C#

```csharp
var client = BluefinDecryptxSDK.TestSDK(null, null);
var decryptList = client.Decrypt().List(null);
Console.WriteLine(decryptList);
```

### Dart

```dart
import 'package:bluefin_decryptx_sdk/BluefinDecryptxSDK.dart';

Future<void> main() async {
  final client = BluefinDecryptxSDK.test();
  final decrypts = await client.Decrypt().list();
  print(decrypts);
}
```

### Elixir

```elixir
alias BluefinDecryptx.Helpers, as: H

sdk = BluefinDecryptx.test()
decrypt = BluefinDecryptx.decrypt(sdk)
records = BluefinDecryptx.Entity.Decrypt.list(decrypt, H.deep(%{}))
IO.inspect(records)
```

### Haskell

```haskell
import qualified SdkClient as Sdk
import VoxgigStruct (Value (..), emptyMap)
import SdkHelpers (jo)

main :: IO ()
main = do
  sdk <- Sdk.testSdk0
  ent <- Sdk.decrypt sdk VNoval
  arg <- emptyMap
  ctrl <- emptyMap
  decrypts <- Sdk.eList ent arg ctrl
  print decrypts
```

### Java

```java
BluefinDecryptxSDK client = BluefinDecryptxSDK.testSDK(null, null);
Object decryptList = client.decrypt(null).list(null, null);
System.out.println(decryptList);
```

### JavaScript

```js
const client = BluefinDecryptxSDK.test()
const decrypts = await client.Decrypt().list()
// decrypts is an array of bare entities populated with mock data
console.log(decrypts)
```

### Kotlin

```kotlin
val client = BluefinDecryptxSDK.testSDK(null, null)
val decryptList = client.decrypt(null).list(null, null)
println(decryptList)
```

### OCaml

```ocaml
let () =
  let client = Sdk_client.test () in
  let result = (Sdk_client.decrypt client Noval).e_list (empty_map ()) Noval in
  print_endline (stringify result)
```

### Perl

```perl
use lib 'perl/lib';
use BluefinDecryptxSDK;

my $client = BluefinDecryptxSDK->test(undef, undef);
my $decrypts = $client->Decrypt->list();
print scalar(@$decrypts), " records\n";
```

### Rust

```rust
use bluefin_decryptx_sdk::{jo, test_sdk, Value};

let client = test_sdk(Value::Noval, Value::Noval);
let decrypts = client.decrypt(Value::Noval).list(Value::Noval, Value::Noval).unwrap();
println!("{:?}", decrypts);
```

### Scala

```scala
val client = BluefinDecryptxSDK.testSDK(null, null)
val decryptList = client.decrypt(null).list(null, null)
println(decryptList)
```

### Swift

```swift
let client = BluefinDecryptxSDK.testSDK(nil, nil)
let decryptList = try client.Decrypt().list(nil, nil)
print(decryptList)
```

### Zig

```zig
const std = @import("std");
const sdk = @import("sdk");
const h = sdk.h;

const client = sdk.test_sdk(h.vnull(), h.vnull());
switch (client.decrypt(h.vnull()).list(h.vnull(), h.vnull())) {
    .ok => |decrypts| std.debug.print("{s}\n", .{h.stringify(decrypts)}),
    .err => |e| std.debug.print("list failed: {s}\n", .{e.msg}),
}
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Python | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| PHP | `voxgig-sdk/bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Golang | `github.com/voxgig-sdk/bluefin-decryptx-sdk/go` | `go get github.com/voxgig-sdk/bluefin-decryptx-sdk/go@latest` |
| Ruby | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Lua | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| C | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Clojure | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| C++ | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| C# | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Dart | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Elixir | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Haskell | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Java | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| JavaScript | `@voxgig-sdk/bluefin-decryptx-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Kotlin | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| OCaml | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Perl | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Rust | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Scala | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Swift | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Zig | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Go CLI | `github.com/voxgig-sdk/bluefin-decryptx-sdk/go-cli` | `go install github.com/voxgig-sdk/bluefin-decryptx-sdk/go-cli/cmd/bluefin-decryptx@latest` |
| Go MCP server | `github.com/voxgig-sdk/bluefin-decryptx-sdk/go-mcp` | `go get github.com/voxgig-sdk/bluefin-decryptx-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { BluefinDecryptxSDK } from '@voxgig-sdk/bluefin-decryptx'

const client = new BluefinDecryptxSDK({
  apikey: process.env.BLUEFIN_DECRYPTX_APIKEY,
})

// List all decrypts (returns Decrypt[])
const decrypts = await client.Decrypt().list()
for (const decrypt of decrypts) {
  console.log(decrypt)
}
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Ruby, Lua, C, Clojure, C++, C#, Dart, Elixir, Haskell, Java, JavaScript, Kotlin, OCaml, Perl, Rust, Scala, Swift, Zig) | `ts/` `py/` `php/` `go/` `rb/` `lua/` `c/` `clojure/` `cpp/` `csharp/` `dart/` `elixir/` `haskell/` `java/` `js/` `kotlin/` `ocaml/` `perl/` `rust/` `scala/` `swift/` `zig/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o bluefin-decryptx-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "bluefin-decryptx": {
      "command": "/abs/path/to/bluefin-decryptx-mcp"
    }
  }
}
```

## Entities

The API exposes 3 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **Decrypt** | The Decrypt entity (create, list). | `/decrypt` |
| **DecryptExt** | The DecryptExt entity (create). | `/decrypt-ext` |
| **Validation** | The Validation entity (create). | `/device/validate` |

The operations available across these entities are **list**, **create** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from bluefindecryptx_sdk import BluefinDecryptxSDK

client = BluefinDecryptxSDK({
    "apikey": os.environ.get("BLUEFIN_DECRYPTX_APIKEY"),
})

# List all decrypts (returns a list, raises on error)
decrypts = client.Decrypt().list()
for decrypt in decrypts:
    print(decrypt)
```

### PHP

```php
<?php
require_once 'bluefindecryptx_sdk.php';

$client = new BluefinDecryptxSDK([
    "apikey" => getenv("BLUEFIN_DECRYPTX_APIKEY"),
]);

// List all decrypts (returns an array; throws on error)
$decrypts = $client->Decrypt()->list();
print_r($decrypts);
```

### Golang

```go
import sdk "github.com/voxgig-sdk/bluefin-decryptx-sdk/go"

client := sdk.NewBluefinDecryptxSDK(map[string]any{
    "apikey": os.Getenv("BLUEFIN_DECRYPTX_APIKEY"),
})

// List all decrypts
decrypts, err := client.Decrypt(nil).List(nil, nil)
if err != nil {
    panic(err)
}
fmt.Println(decrypts)
```

### Ruby

```ruby
require_relative "BluefinDecryptx_sdk"

client = BluefinDecryptxSDK.new({
  "apikey" => ENV["BLUEFIN_DECRYPTX_APIKEY"],
})

# List all decrypts (returns an Array; raises on error)
decrypts = client.Decrypt.list
puts decrypts
```

### Lua

```lua
local sdk = require("bluefin-decryptx_sdk")

local client = sdk.new({
  apikey = os.getenv("BLUEFIN_DECRYPTX_APIKEY"),
})

-- List all decrypts
local decrypts, err = client:Decrypt():list()
print(decrypts)
```

### C

```c
#include "core/api.h"

BluefinDecryptxSDK* client = bluefin_decryptx_sdk_new(cmap(1,
    "apikey", v_str(getenv("BLUEFIN_DECRYPTX_APIKEY"))));
PNError* err = NULL;

Entity* decrypt = bluefin_decryptx_decrypt(client, NULL);

// List all decrypts (returns a List, sets *err on failure)
voxgig_value* decrypts = decrypt->vt->list(decrypt, NULL, NULL, &err);
for (size_t i = 0; i < (size_t)voxgig_size(decrypts); i++) {
    printf("%s\n", voxgig_to_json(voxgig_getelem(decrypts, v_int(i), NULL)));
}
```

### Clojure

```clojure
(require '[sdk.api :as api]
         '[sdk.entity.decrypt :as e-decrypt]
         '[voxgig.struct :as vs])

(def client (api/make-sdk (vs/jm "apikey" (System/getenv "BLUEFIN_DECRYPTX_APIKEY"))))

;; List all decrypts (returns a vector, raises on error)
(doseq [decrypt (e-decrypt/list (api/decrypt client nil) nil nil)]
  (println decrypt))
```

### C++

```cpp
#include <cstdlib>
#include "core/sdk.hpp"

using namespace sdk;

const char* apikey = std::getenv("BLUEFIN_DECRYPTX_APIKEY");
auto client = std::make_shared<BluefinDecryptxSDK>(vmap({
    {"apikey", Value(apikey ? apikey : "")},
}));

// List all decrypts (returns a Value list, throws on error)
Value decrypts = client->decrypt()->list(Value::undef(), Value::undef());
for (const auto& decrypt : *decrypts.as_list()) {
  std::cout << Struct::jsonify(decrypt) << std::endl;
}
```

### C#

```csharp
using BluefinDecryptxSdk;

var client = new BluefinDecryptxSDK(new Dictionary<string, object?>
{
    ["apikey"] = Environment.GetEnvironmentVariable("BLUEFIN_DECRYPTX_APIKEY"),
});

// List all decrypts (returns object?, an aggregate list; raises on error)
var decryptList = client.Decrypt().List(null);
Console.WriteLine(decryptList);
```

### Dart

```dart
import 'dart:io';
import 'package:bluefin_decryptx_sdk/BluefinDecryptxSDK.dart';

Future<void> main() async {
  final client = BluefinDecryptxSDK({
    'apikey': Platform.environment['BLUEFIN_DECRYPTX_APIKEY'],
  });

  // List all decrypts (returns a list of entities, throws on error)
  final decrypts = await client.Decrypt().list();
  for (final item in decrypts) {
    print(item.data());
  }
}
```

### Elixir

```elixir
alias BluefinDecryptx.Helpers, as: H

sdk = BluefinDecryptx.new(H.deep(%{"apikey" => System.get_env("BLUEFIN_DECRYPTX_APIKEY")}))

decrypt = BluefinDecryptx.decrypt(sdk)

# List all decrypt records (raises on error)
records = BluefinDecryptx.Entity.Decrypt.list(decrypt)
IO.inspect(records)
```

### Haskell

```haskell
import System.Environment (lookupEnv)
import qualified SdkClient as Sdk
import VoxgigStruct (Value (..), emptyMap)
import SdkHelpers (jo)

main :: IO ()
main = do
  mkey <- lookupEnv "BLUEFIN_DECRYPTX_APIKEY"
  opts <- jo [("apikey", maybe VNoval VStr mkey)]
  sdk <- Sdk.newSdk opts

  -- List all decrypts (returns a list Value, raises on error)
  ent <- Sdk.decrypt sdk VNoval
  match <- emptyMap
  ctrl <- emptyMap
  decrypts <- Sdk.eList ent match ctrl
  print decrypts
```

### Java

```java
import voxgig.bluefindecryptxsdk.core.BluefinDecryptxSDK;

Map<String, Object> options = new java.util.LinkedHashMap<>();
options.put("apikey", System.getenv("BLUEFIN_DECRYPTX_APIKEY"));
BluefinDecryptxSDK client = new BluefinDecryptxSDK(options);

// List all decrypts (returns Object, an aggregate list; raises on error)
Object decryptList = client.decrypt(null).list(null, null);
System.out.println(decryptList);
```

### JavaScript

```js
const { BluefinDecryptxSDK } = require('@voxgig-sdk/bluefin-decryptx-js')

const client = new BluefinDecryptxSDK({
  apikey: process.env.BLUEFIN_DECRYPTX_APIKEY,
})

// List all decrypts (returns an array)
const decrypts = await client.Decrypt().list()
for (const decrypt of decrypts) {
  console.log(decrypt)
}
```

### Kotlin

```kotlin
import voxgig.bluefindecryptxsdk.core.BluefinDecryptxSDK

val client = BluefinDecryptxSDK(mutableMapOf<String, Any?>(
    "apikey" to System.getenv("BLUEFIN_DECRYPTX_APIKEY"),
))

// List all decrypts (returns Any?, an aggregate list; raises on error)
val decryptList = client.decrypt(null).list(null, null)
println(decryptList)
```

### OCaml

```ocaml
open Voxgig_struct
open Sdk_helpers

let () =
  let client = Sdk_client.make (jo [("apikey", Str (Sys.getenv "BLUEFIN_DECRYPTX_APIKEY"))]) in
  (* List all decrypt records (returns a List value; raises on error) *)
  let decrypts = (Sdk_client.decrypt client Noval).e_list (empty_map ()) Noval in
  (match decrypts with List items -> List.iter (fun r -> print_endline (stringify r)) !items | _ -> ());
```

### Perl

```perl
use lib 'perl/lib';
use BluefinDecryptxSDK;

my $client = BluefinDecryptxSDK->new({
    'apikey' => $ENV{'BLUEFIN_DECRYPTX_APIKEY'},
});

# List all decrypts (returns an arrayref; dies on error)
my $decrypts = $client->Decrypt->list;
for my $decrypt (@$decrypts) {
    print "$decrypt->{id}\n";
}
```

### Rust

```rust
use bluefin_decryptx_sdk::{jo, BluefinDecryptxSDK, Value};

let client = BluefinDecryptxSDK::new(jo(vec![
    ("apikey", Value::str(std::env::var("BLUEFIN_DECRYPTX_APIKEY").unwrap_or_default())),
]));

// List all decrypts (returns a Value::List, Err on failure)
let decrypts = client.decrypt(Value::Noval).list(Value::Noval, Value::Noval).unwrap();
if let Value::List(items) = &decrypts {
    for decrypt in items.borrow().iter() {
        println!("{:?}", decrypt);
    }
}
```

### Scala

```scala
import voxgig.bluefindecryptxsdk.core.BluefinDecryptxSDK

val options = new java.util.LinkedHashMap[String, Object]()
options.put("apikey", System.getenv("BLUEFIN_DECRYPTX_APIKEY"))
val client = new BluefinDecryptxSDK(options)

// List all decrypts (returns Object, an aggregate list; raises on error)
val decryptList = client.decrypt(null).list(null, null)
println(decryptList)
```

### Swift

```swift
import BluefinDecryptxSdk

let options = VMap()
options.entries["apikey"] = .string(
    ProcessInfo.processInfo.environment["BLUEFIN_DECRYPTX_APIKEY"] ?? "")
let client = BluefinDecryptxSDK(options)

// List all decrypts (returns a Value list, throws on error)
let decryptList = try client.Decrypt().list(nil, nil)
for decrypt in decryptList.asList?.items ?? [] {
    print(decrypt)
}
```

### Zig

```zig
const std = @import("std");
const sdk = @import("sdk");
const h = sdk.h;

const client = sdk.BluefinDecryptxSDK.new(h.jo(&.{
    .{ "apikey", h.vstr(std.posix.getenv("BLUEFIN_DECRYPTX_APIKEY") orelse "") },
}));

// List all decrypts (Ok is a Value array, .err on failure)
switch (client.decrypt(h.vnull()).list(h.vnull(), h.vnull())) {
    .ok => |decrypts| std.debug.print("{s}\n", .{h.stringify(decrypts)}),
    .err => |e| std.debug.print("list failed: {s}\n", .{e.msg}),
}
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Ruby:**
```ruby
result = client.direct({
  "path" => "/api/resource/{id}",
  "method" => "GET",
  "params" => { "id" => "example" },
})
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**C:**
```c
PNError* err = NULL;
voxgig_value* result = sdk_direct(client, cmap(3,
    "path", v_str("/api/resource/{id}"),
    "method", v_str("GET"),
    "params", cmap(1, "id", v_str("example"))), &err);
```

**Clojure:**
```clojure
(def result
  (api/direct client
    (vs/jm "path" "/api/resource/{id}"
           "method" "GET"
           "params" (vs/jm "id" "example"))))
```

**C++:**
```cpp
Value result = client->direct(vmap({
    {"path", Value("/api/resource/{id}")},
    {"method", Value("GET")},
    {"params", vmap({{"id", Value("example")}})},
}));
```

**C#:**
```csharp
var result = client.Direct(new Dictionary<string, object?>
{
    ["path"] = "/api/resource/{id}",
    ["method"] = "GET",
    ["params"] = new Dictionary<string, object?> { ["id"] = "example" },
});
```

**Dart:**
```dart
final result = await client.direct({
  'path': '/api/resource/{id}',
  'method': 'GET',
  'params': {'id': 'example'},
});
```

**Elixir:**
```elixir
result = BluefinDecryptx.direct(sdk, BluefinDecryptx.Helpers.deep(%{
  "path" => "/api/resource/{id}",
  "method" => "GET",
  "params" => %{"id" => "example"}
}))
```

**Haskell:**
```haskell
import qualified SdkClient as Sdk
import qualified SdkFeatures as F
import VoxgigStruct (Value (..))
import SdkHelpers (jo)

main :: IO ()
main = do
  sdk <- Sdk.newSdk0
  params <- jo [("id", VStr "example")]
  args <- jo [("path", VStr "/api/resource/{id}"), ("method", VStr "GET"), ("params", params)]
  result <- F.direct sdk args
  print result
```

**Java:**
```java
Map<String, Object> result = client.direct(Map.of(
    "path", "/api/resource/{id}",
    "method", "GET",
    "params", Map.of("id", "example")));
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Kotlin:**
```kotlin
val result = client.direct(mutableMapOf<String, Any?>(
    "path" to "/api/resource/{id}",
    "method" to "GET",
    "params" to mapOf("id" to "example")))
```

**OCaml:**
```ocaml
let result = Sdk_client.direct client (jo [
    ("path", Str "/api/resource/{id}");
    ("method", Str "GET");
    ("params", jo [("id", Str "example")]);
]) in
ignore result
```

**Perl:**
```perl
my $result = $client->direct({
    'path' => '/api/resource/{id}',
    'method' => 'GET',
    'params' => { 'id' => 'example' },
});
```

**Rust:**
```rust
let result = client.direct(jo(vec![
    ("path", Value::str("/api/resource/{id}")),
    ("method", Value::str("GET")),
    ("params", jo(vec![("id", Value::str("example"))])),
]));
```

**Scala:**
```scala
val result = client.direct(java.util.Map.of(
    "path", "/api/resource/{id}",
    "method", "GET",
    "params", java.util.Map.of("id", "example")))
```

**Swift:**
```swift
let result = client.direct(VMap([
    ("path", .string("/api/resource/{id}")),
    ("method", .string("GET")),
    ("params", .map([("id", .string("example"))])),
]))
```

**Zig:**
```zig
const result = client.direct(h.jo(&.{
    .{ "path", h.vstr("/api/resource/{id}") },
    .{ "method", h.vstr("GET") },
    .{ "params", h.jo(&.{.{ "id", h.vstr("example") }}) },
}));
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **TestFeature** | In-memory mock transport for testing without a live server |

Pass custom features via the `extend` option at construction time.

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Ruby](rb/README.md)
- [Lua](lua/README.md)
- [C](c/README.md)
- [Clojure](clojure/README.md)
- [C++](cpp/README.md)
- [C#](csharp/README.md)
- [Dart](dart/README.md)
- [Elixir](elixir/README.md)
- [Haskell](haskell/README.md)
- [Java](java/README.md)
- [JavaScript](js/README.md)
- [Kotlin](kotlin/README.md)
- [OCaml](ocaml/README.md)
- [Perl](perl/README.md)
- [Rust](rust/README.md)
- [Scala](scala/README.md)
- [Swift](swift/README.md)
- [Zig](zig/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

- Upstream API: [https://secure-prod.decryptx.com/api](https://secure-prod.decryptx.com/api)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Decryptx APIs OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
