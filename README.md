# BluefinDecryptx SDK

Decryptx APIs client, generated from the OpenAPI spec.

This page describes the Bluefin Partner Application Programming Interface (API) for the Decryptx service.

Learn more about Decryptx APIs at [secure-prod.decryptx.com](https://secure-prod.decryptx.com).

This is an unofficial SDK for the Decryptx APIs public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

> TypeScript, Python, PHP, Golang, Ruby, Lua SDKs, a CLI, an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

## Entities, not endpoints

This SDK exposes the API as a small set of **semantic entities** — Decrypt, DecryptExt and Validation — that you
call directly, instead of assembling URL paths and query strings. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `create`):

```ts
const client = new BluefinDecryptxSDK()
const items = await client.Decrypt().list()
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
const client = BluefinDecryptxSDK.test()
const decrypts = await client.Decrypt().list()
// decrypts is an array of bare Decrypt records populated with mock data
console.log(decrypts)
```

### Python

```python
client = BluefinDecryptxSDK.test()
decrypts = client.Decrypt().list()
print(decrypts)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = BluefinDecryptxSDK::test([
    "entity" => ["decrypt" => ["test01" => []]],
]);
$decrypts = $client->Decrypt()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.Decrypt(nil).List(
    nil, nil,
)
```

### Ruby

```ruby
# Seed fixture data so offline calls resolve without a live server.
client = BluefinDecryptxSDK.test({
  "entity" => { "decrypt" => { "test01" => {} } },
})
decrypts = client.Decrypt.list()
```

### Lua

```lua
local client = sdk.test()
local results, err = client:Decrypt():list()
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Python | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| PHP | `voxgig-sdk/bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Golang | `github.com/voxgig-sdk/bluefin-decryptx-sdk/go` | `go get github.com/voxgig-sdk/bluefin-decryptx-sdk/go@latest` |
| Ruby | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Lua | `voxgig-sdk-bluefin-decryptx` | publish pending — [install from git tag](https://github.com/voxgig-sdk/bluefin-decryptx-sdk/releases) |
| Go CLI | `github.com/voxgig-sdk/bluefin-decryptx-sdk/go-cli` | `go install github.com/voxgig-sdk/bluefin-decryptx-sdk/go-cli/cmd/bluefin-decryptx@latest` |
| Go MCP server | `github.com/voxgig-sdk/bluefin-decryptx-sdk/go-mcp` | `go get github.com/voxgig-sdk/bluefin-decryptx-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { BluefinDecryptxSDK } from '@voxgig-sdk/bluefin-decryptx'

const client = new BluefinDecryptxSDK({
  apikey: process.env.BLUEFIN_DECRYPTX_APIKEY,
})

// List all decrypts (returns Decrypt[])
const decrypts = await client.Decrypt().list()
for (const decrypt of decrypts) {
  console.log(decrypt)
}
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Ruby, Lua) | `ts/` `py/` `php/` `go/` `rb/` `lua/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o bluefin-decryptx-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "bluefin-decryptx": {
      "command": "/abs/path/to/bluefin-decryptx-mcp"
    }
  }
}
```

## Entities

The API exposes 3 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **Decrypt** | The Decrypt entity (create, list). | `/decrypt` |
| **DecryptExt** | The DecryptExt entity (create). | `/decrypt-ext` |
| **Validation** | The Validation entity (create). | `/device/validate` |

The operations available across these entities are **list**, **create** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from bluefindecryptx_sdk import BluefinDecryptxSDK

client = BluefinDecryptxSDK({
    "apikey": os.environ.get("BLUEFIN_DECRYPTX_APIKEY"),
})

# List all decrypts (returns a list, raises on error)
decrypts = client.Decrypt().list()
for decrypt in decrypts:
    print(decrypt)
```

### PHP

```php
<?php
require_once 'bluefindecryptx_sdk.php';

$client = new BluefinDecryptxSDK([
    "apikey" => getenv("BLUEFIN_DECRYPTX_APIKEY"),
]);

// List all decrypts (returns an array; throws on error)
$decrypts = $client->Decrypt()->list();
print_r($decrypts);
```

### Golang

```go
import sdk "github.com/voxgig-sdk/bluefin-decryptx-sdk/go"

client := sdk.NewBluefinDecryptxSDK(map[string]any{
    "apikey": os.Getenv("BLUEFIN_DECRYPTX_APIKEY"),
})

// List all decrypts
decrypts, err := client.Decrypt(nil).List(nil, nil)
if err != nil {
    panic(err)
}
fmt.Println(decrypts)
```

### Ruby

```ruby
require_relative "BluefinDecryptx_sdk"

client = BluefinDecryptxSDK.new({
  "apikey" => ENV["BLUEFIN_DECRYPTX_APIKEY"],
})

# List all decrypts (returns an Array; raises on error)
decrypts = client.Decrypt.list
puts decrypts
```

### Lua

```lua
local sdk = require("bluefin-decryptx_sdk")

local client = sdk.new({
  apikey = os.getenv("BLUEFIN_DECRYPTX_APIKEY"),
})

-- List all decrypts
local decrypts, err = client:Decrypt():list()
print(decrypts)
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Ruby:**
```ruby
result = client.direct({
  "path" => "/api/resource/{id}",
  "method" => "GET",
  "params" => { "id" => "example" },
})
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **TestFeature** | In-memory mock transport for testing without a live server |

Pass custom features via the `extend` option at construction time.

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Ruby](rb/README.md)
- [Lua](lua/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

- Upstream API: [https://secure-prod.decryptx.com/api](https://secure-prod.decryptx.com/api)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Decryptx APIs OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
